The seeding process currently creates incomplete skeleton records that require manual intervention. As seen in the admin panel:
- Main image is missing and Gallery displays (0) images.
- Long Description is blank ("Full product description...").
- Tags are generic and inaccurate (e.g., showing "5g, flagship" for Huntsman Epoxy Resin).
- The admin is still required to manually click "Auto-Generate" item by item.

Please overhaul the seeding and product generation process to fulfill these strict requirements automatically:

### 1. Fully Automated Background Batch Generation (No Manual Clicks)
- Execute a background script (or build/migration task) that automatically processes ALL catalog products end-to-end.
- Do NOT rely on the user clicking "Seed" or "Auto-Generate" manually for each item. When the script runs, it must complete 100% of the product details, images, and SEO metadata automatically in the database.

### 2. Complete Content & Specification Filling
For every single catalog item (LawMate, Esonic, Carbon Fiber, Huntsman Epoxy, Custom Batteries, and GPS Trackers):
- **Long Description:** Generate a detailed HTML description including technical specifications, usage guidelines, chemical/physical properties (for epoxies/composites), or hardware features (for surveillance/GPS).
- **Contextual Tags:** Replace default mobile tags ("5g", "flagship") with category-accurate tags (e.g., for Epoxy: "epoxy, resin, ly5052, adhesive, industrial, huntsman"; for Surveillance: "lawmate, covert camera, security, spy gear").
- **Pricing & Stock:** Assign realistic default PKR values or set appropriate "Get Quote / Inquiry" modes for industrial items.

### 3. Strict 5-Image Local Scraping Rule (1 Main + 4 Gallery)
- For EVERY product, programmatically fetch and download:
  1. One (1) Primary/Main Image (100% model match).
  2. Four (4) Gallery/Additional Images (angles, close-ups, packaging, or spec diagrams).
- Process and save ALL 5 images directly to the server directory (`/public/uploads/products/` and `/public/uploads/products/gallery/`) as WebP files.
- Store the relative server paths in the `products` and `product_images` database tables so every product opens with a main photo and a 4-image gallery fully loaded.

### 4. Verification Step
- Run this full batch process now across all catalog items.
- Ensure that when opening any product in the Edit view, all fields (Main Image, Gallery 4-pack, Long Description, Tags, Category, and SEO Meta) are 100% filled and ready.